import { addBall, hit } from "./Dash";

describe("Dash.js", () => {
  describe("hit()", () => {
    it("reset balls and strikes to 0", () => {
      // expect(hit()).toBe(Im not sure)
    });
  });
});
